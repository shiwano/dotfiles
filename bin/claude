#!/usr/bin/env bash

set -euo pipefail

main() {
	local args=(
		--allow-write "$HOME/dotfiles/dot.config/claude"
		--allow-write "$HOME/.config/claude"
		--allow-write "$HOME/.codex"
	)
	exec mise exec -- util-agent-sandbox-exec "${args[@]}" -- claude "$@"
}

main "$@"
